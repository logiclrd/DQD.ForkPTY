#!/bin/tcsh

set arch=`uname -m`

switch ( $arch )
	case x86_64:
	case amd64:
		echo osx-x64
		breaksw
	case aarch64
		echo osx-arm64
		breaksw
	default:
		echo Unrecognized: $arch >&2
		exit 1
endsw
